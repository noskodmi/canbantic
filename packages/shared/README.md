# @kanbantic/shared

Provider-agnostic types, ABIs, zod schemas, viem clients, and ENS helpers
shared between `apps/web` and `apps/worker`.

Phase 0 ships only a smoke `version()` export. Phase 1 lands ABIs + ENS helpers.
